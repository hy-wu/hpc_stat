{
  "name": "hpc-stat",
  "version": "2.0.0",
  "type": "module",
  "description": "Unified GPU + LLM parameter workbench",
  "scripts": {
    "prebuild": "node scripts/sync-public-data.mjs",
    "dev": "node scripts/sync-public-data.mjs && astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check && tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "astro": "^5.18.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^5.9.3"
  }
}
